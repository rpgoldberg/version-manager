FROM node:20-alpine

WORKDIR /app

# Install bash and curl for test scripts and health checks
RUN apk add --no-cache bash curl

# Copy package files
COPY package*.json ./

# Install all dependencies (including dev dependencies for testing)
RUN npm install

# Copy application code and version.json
COPY . .

# Ensure test output directory exists
RUN mkdir -p /app/test-output

# For integration testing, we need the service to start, not run tests
# The integration test runner will execute tests separately
CMD ["npm", "start"]